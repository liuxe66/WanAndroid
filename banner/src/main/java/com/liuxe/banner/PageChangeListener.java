package com.liuxe.banner;

public interface PageChangeListener {
    void scrollToLeft(int position, float positionOffset, int positionOffsetPixels);
    void scrollToRight(int position, float positionOffset, int positionOffsetPixels);
}
